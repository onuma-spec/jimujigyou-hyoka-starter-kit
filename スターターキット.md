# 事務事業優先度評価ツール 自治体追加スターターキット

このファイルはAIコーディング支援（Claude Code等）が最初に読むためのオンボーディング文書です。
人間の operator は、AIコーディング支援（Claude Code等）を開いて「このリポジトリ（`https://github.com/onuma-spec/jimujigyou-hyoka-starter-kit`）をcloneして、スターターキット.mdを読んで、〇〇市(区町村)を追加したい」と伝えるだけで作業を開始できます（gitが使えない環境でもAIがZIPを直接取得して展開できるので、事前のダウンロード作業は不要です。URLの代わりにこのファイルと下記の同梱ファイルを直接渡しても構いません）。

同梱している`fixed_prompt.md`・`build_html.py`等のファイル群は、GitHub上でMITライセンス（自由に閲覧・コピー・改変・再配布可能）で公開されているものと同一内容です。ご自由に使っていただいて構いません。

## このプロジェクトの目的

各自治体が公表している事務事業評価データ（PDF/Excel）を、住民が「続行・廃止・見直し」の3択で評価し、投票として共有できる単一HTMLツールに変換する。既に7自治体（西宮市・北本市・湖西市・品川区・富田林市・聖籠町・一宮市）で稼働済み。今回はこれと同じものを、新しい自治体向けに作る。

## 権威ある手順書

**`fixed_prompt.md`（同じフォルダ内）が唯一の権威ある本体です。** データの型（標準列・タグ定義）・4フェーズの作業フロー・品質チェック手順に加え、冒頭の「環境設定」で①ハブ運用（制作者本人）／②独立運用の両方に対応しています。このスターターキット.mdはfixed_prompt.mdの内容を要約・重複するものではなく、**独立ユーザーがfixed_prompt.mdをどう読み進めればよいかの道案内**（環境設定で②を選ぶ案内・同梱ファイルの使い方・実地テスト済みの手順順序）です。作業中に迷ったら必ずfixed_prompt.mdの該当フェーズに戻って確認してください。

## リポジトリの取得

利用者からリポジトリURL（`https://github.com/onuma-spec/jimujigyou-hyoka-starter-kit`）だけを渡された場合は、以下の手順でAI自身が取得する。人間側の作業は「AIコーディング支援を、デスクトップ等の分かりやすい場所で開いてURLを伝える」だけでよく、フォルダを手動で用意したりZIPを手動展開したりする必要はない。

1. `git --version`でgitが使えるか確認する
2. **gitが使える場合**：`git clone https://github.com/onuma-spec/jimujigyou-hyoka-starter-kit.git`を実行する（今後`git pull`で更新を追いやすいメリットがある）
3. **gitが使えない場合**：インストールを促す必要はない。GitHubのZIP直接ダウンロードURL（`https://github.com/onuma-spec/jimujigyou-hyoka-starter-kit/archive/refs/heads/main.zip`）を`curl`（Bash）または`Invoke-WebRequest`（PowerShell）で取得し、`unzip`（Bash）または`Expand-Archive`（PowerShell）で展開する。どちらもOS標準搭載のコマンドで追加インストール不要
4. 展開・clone先のフォルダに移動し、以降はこのファイル（スターターキット.md）を読み込んで進める

## 事前準備（環境構築）

**Claude Codeへ**：作業を始める前に、まず利用者に「環境構築(Python・必要なライブラリのインストール)はご自身で済んでいますか？それともこちらで確認・インストールしましょうか？」と確認してください。「自分で済んでいる」という返答であれば下記が整っていることを前提に次のステップへ進み、「任せる」という返答であればバージョン確認・インストールコマンドの実行から一緒に進めてください。

最終的に使える状態にしておく必要があるもの：

- **VSCode＋Claude Code**（またはコード実行可能な同等のAIコーディング支援）：チャットで依頼するだけでなく、実際にPythonスクリプトを書いて実行できることが必須（PDFの読み取り・座標ベース抽出は試行錯誤が必要なため、コード実行できないチャットAIだけでは代用できません）
- **Python 3系**：インストール済みであること
- **必要なライブラリ**：以下をコマンドラインで一度インストールする
  ```
  pip install pdfminer.six pymupdf openpyxl pandas
  ```
  - `pdfminer.six`：PDFからのテキスト抽出（`extract_simple.py`・`coord_extract_utils.py`で使用）
  - `pymupdf`（fitz）：文字化けするPDFの代替抽出、および数値グリッドの座標ベース抽出（`coord_extract_utils.py`内で使用）
  - `openpyxl`：元データがExcelの場合の読み込み
  - `pandas`：Excelデータの集計・整形

## 独立運用のための読み替え（重要）

fixed_prompt.mdは、このツールの制作者本人の環境・アカウント向けと、独立運用向けの両方に対応できるよう書かれています。**今回は制作者から完全に独立した別プロジェクトとして運用するため**、以下の3点を読み替えてください。

1. **環境設定は「②独立運用」を選ぶ**：fixed_prompt.md冒頭に「環境設定（作業開始前に必ずどちらか選ぶ）」という節があり、①ハブ運用（制作者本人）／②独立運用の2プリセットが表になっています。**必ず②を選び**、以降本文中に出てくる作業ディレクトリ・GitHub公開先・Supabase URL/KEY・population参照元は、すべて②列の値で読み替えてください。①列（制作者本人の作業フォルダ・ハブリポジトリ・共有Supabase・外部フォルダ）には一切アクセスしません。具体的には：
   - **作業ディレクトリ**：ステップ0でclone（またはZIP展開）した、ご自身の作業フォルダになります。fixed_prompt.md中の「01_元データ\」「02_抽出スクリプト\」等のサブフォルダ名も、ご自身の作業フォルダを起点に解釈してください（同じ分割を再現しても構いませんし、フラットに1フォルダにまとめても構いません）
   - **GitHub**：ご自身のアカウントで新規リポジトリを作成し、そこにHTMLをアップロードしてください（既存の`jimujigyou-hyoka`ハブへの登録は行わない＝完全に独立したツールとして運用する）
   - **Supabase**：投票機能（`LABELS.enable_voting`）を使う場合のみ必要。[supabase.com](https://supabase.com)で新規に無料プロジェクトを作成し、`build_html.py`内の`SUPABASE_URL`・`SUPABASE_ANON_KEY`をご自身のプロジェクトの値に書き換える。**`enable_voting`を`False`にした場合はこのSupabase作業自体が丸ごと不要**で、GitHubへのアップロードのみで公開が完結する（投票機能なしで、個人のLocalStorage評価・全事務事業データCSVダウンロードのみのツールになる）。テーブルも以下のSQLで新規作成する必要がある（SupabaseのSQL Editorで実行）：
     ```sql
     CREATE TABLE ratings (
       id              bigserial PRIMARY KEY,
       municipality_id text        NOT NULL,
       event_no        text        NOT NULL,
       rating          text        NOT NULL,
       submitted_at    timestamptz NOT NULL DEFAULT now()
     );

     ALTER TABLE ratings ENABLE ROW LEVEL SECURITY;
     CREATE POLICY "anyone can insert" ON ratings FOR INSERT TO anon WITH CHECK (true);
     CREATE POLICY "anyone can select" ON ratings FOR SELECT TO anon USING (true);
     GRANT SELECT, INSERT ON TABLE public.ratings TO anon;
     GRANT USAGE, SELECT ON SEQUENCE public.ratings_id_seq TO anon;
     ```
     （UPDATE・DELETEはanonに許可しない＝運用するご本人だけがSQL Editorから行う設計。個人情報は一切収集しないテーブル設計なので、この点は変更せずそのまま踏襲することを推奨）
   - **population**：同梱の`population_latest.csv`（市区町村コード・市区町村名・人口数_人の3列、出典：デジタル庁 地方財政ダッシュボード）から対象自治体の値を使ってください
2. **同梱ファイルの配置**：今回共有している`extract_simple.py`・`extract_coordinate.py`・`coord_extract_utils.py`・`build_html.py`・`story_gen_prompt.md`は、元のプロジェクトでは別々のサブフォルダに分かれていますが、**今回はこのフォルダに直接まとめて共有しています**。fixed_prompt.md内でサブフォルダ付きのパス表記が出てきても、実際にはこのフォルダ直下にあるものとして読み替えてください
3. **ファイル名の対応**：fixed_prompt.mdは制作者本人の内部ファイル名（自治体名がそのまま付いたもの）で書かれていますが、このキットでは役割を直接表す汎用的な名前に変更しています。fixed_prompt.md中に以下の名前が出てきたら、右側の同梱ファイルに読み替えてください。
   | fixed_prompt.md中の表記 | このキットでの実際のファイル名 |
   |---|---|
   | `build_kitamoto_v3.py`（またはbuild_{自治体名}_v3.py） | `build_html.py` |
   | `extract_kitamoto_v2.py` | `extract_simple.py` |
   | `extract_tondabayashi_v2.py` | `extract_coordinate.py` |

## 同梱ファイルと使い方

| ファイル | 役割 |
|---|---|
| `fixed_prompt.md` | 権威ある作業手順書（上記参照） |
| `build_html.py` | ビルド用Pythonテンプレート（フェーズ③・HTML生成）。全自治体共通のHTML/CSS/JSロジックを持つ。**これをコピーして`build_{自治体名}.py`を作り、`LABELS`辞書を書き換える**。ロジックの大部分は変更不要だが、**「📖 カードの説明」画面（`renderGuide()`関数）は`LABELS`の外にあり、別途手動で対象自治体向けに書き換える必要がある**（コピー元自治体の文言・変換表が固定文字列で残っているため。詳細は下記「作業手順」ステップ5参照）。カードの各セクション（事業番号・担当課／タグ／目的・事業内容／決算額・人件費／成果／廃止した場合の影響）は`LABELS.show_*`フラグで個別に表示・非表示を選べる（データの有無ではなく制作者の好みで決めてよい。詳細は`LABELS`辞書内のコメント参照） |
| `extract_simple.py` | PDFがシンプルな表形式で、pdfminerでそのままテキストが正しい順序で読める場合の標準的な抽出スクリプト例（フェーズ①） |
| `extract_coordinate.py` | PDFのレイアウトが崩れている（選択式の帳票、座標がずれた表組み等）場合の応用例（フェーズ①）。`coord_extract_utils.py`を使った座標ベース抽出の実装例 |
| `coord_extract_utils.py` | 座標ベース抽出が必要な場合に使う汎用関数群（`find_stamps`/`find_label_runs`等） |
| `story_gen_prompt.md` | 評価カードの要約文（目的・事業内容／成果／廃止した場合の影響）をAIに生成させる際のプロンプトテンプレート（フェーズ②）。**必ずこの最新版をそのまま使うこと**（古いコピーは既知のバグを再発させる） |
| `population_latest.csv` | 全国1,741市区町村の人口（市区町村コード・市区町村名・人口数_人の3列、2024年度＝令和6年度、出典：デジタル庁 地方財政ダッシュボード）。`LABELS.population`はWeb検索や元PDFの受益者数等ではなく、このファイルの該当市区町村の値を使うこと |
| `patch_show_labels.py` | 公開後に`show_*`（評価カードの表示項目）だけを、`build_html.py`を再実行せずに変更するCLIツール。データには一切触れない。使い方：`python patch_show_labels.py <html_file> --show-desc false` |
| `patch_show_labels.html` | 同上のブラウザ版。ダブルクリックで開き、ファイルを選ぶ→チェックボックスで表示項目を選ぶ→実行、という操作で使える（プログラミング不要）。バックアップの保存・復元機能付き |

完成品の見本は、実際に公開されている[北本市版](https://onuma-spec.github.io/jimujigyou-hyoka/kitamoto_index.html)を参照してください。

## 生成される成果物ファイル一覧

作業を進めるとフォルダ内にファイルが次々に増えていきます。上記の同梱ファイルとは別に、以下のファイルが作業の中で新たに生成されます。「このファイルは何だったか」を後から確認したいときに参照してください（サブフォルダ分けするかフラットにまとめるかに関わらず、役割・生成順序は変わりません）。

| ファイル名 | 由来 | 内容 | 生成されるタイミング |
|---|---|---|---|
| `{自治体名}_master.csv` | 都度生成 | 元PDF/Excelから抽出した生データ（標準列にマッピング済み） | フェーズ①：`extract_simple.py`または`extract_coordinate.py`を対象自治体向けに書き換えて実行した結果 |
| `{自治体名}_master.csv`（列追加後・同じファイル） | 都度生成（上書き） | `story_p1`（目的・事業内容）・`story_p2`（成果）のAI要約列が追加された状態 | フェーズ②：`story_gen_prompt.md`をプロンプトに使い、その場で書く生成スクリプトを実行した結果 |
| `{自治体名}_classified_all.json` | 都度生成 | 13分類（支援対象×提供形態）のAI分類結果 | フェーズ③冒頭：サブエージェントによるバッチ分類 |
| `{自治体名}_revised.json` | 都度生成 | 上記のうち再判定した差分のみ | フェーズ③冒頭：分類結果の質的レビュー後 |
| `build_{自治体名}.py` | 都度生成（`build_html.py`のコピー＋編集） | 対象自治体向けのビルドスクリプト（`LABELS`辞書・「📖 カードの説明」画面の文言を書き換え済み） | フェーズ③ |
| ビルド直後のHTML | 都度生成 | `python build_{自治体名}.py`を実行した直後の出力（ファイル名は`LABELS['output_file']`で指定した名前） | フェーズ③ |
| `index.html`（GitHubアップロード用） | 都度生成（上記HTMLをリネーム） | 公開用の最終ファイル。独立運用では単独リポジトリのため`index.html`にリネームする（下記「6. フェーズ④：公開」参照） | フェーズ④ |

## 作業手順（0〜6）

人間（👤）・AI単独（🤖）・要確認の協働作業（🤝）の3種類で役割を示す。2026-08-04の実地テスト（朝来市）で検証済みの手順。

### 0. 準備
- 👤 AIコーディング支援（コード実行可能な環境）を開く。迷ったらデスクトップやドキュメント直下など、分かりやすい場所で構わない（`git clone`がその中に`jimujigyou-hyoka-starter-kit/`フォルダを自動作成する）
- 👤 「`https://github.com/onuma-spec/jimujigyou-hyoka-starter-kit` をcloneして、スターターキット.mdを読んで、〇〇市を追加したい」と伝える
- 🤖 gitの有無を確認し、cloneまたはZIP取得・展開を実行（上記「リポジトリの取得」参照）

### 1. キックオフ〜環境確認
- 🤖 AIがスターターキット.md・fixed_prompt.mdを読み込む
- 🤖 fixed_prompt.md冒頭「環境設定」のプリセット②（独立運用）を選ぶ（本ファイル「独立運用のための読み替え」参照。以降このプリセットの値を前提に進める）
- 🤝 AI→人間：「環境構築（Python・ライブラリ）は済んでいますか？」と確認
- 🤖 （任せる場合）バージョン確認・pip installを実行

### 2. 対象自治体・元データの確認
- 対象自治体名はステップ0のキックオフで既に伝わっているため、ここで改めて聞き直す必要はない
- 🤝 AI→人間：元データ（PDF/Excel）を持っているか確認
- （持っていない場合）🤖 AIがWeb検索で公表ページの候補を探す。候補が見つかった時点で、同じ公表シリーズにもっと新しい年度のページが存在しないかを能動的に確認する（自治体は年度ごとに別URLで公表することが多い）
- 🤝 AI→人間：見つけた候補を提示し確認を取る（勝手に使わない）

### 3. フェーズ①：取得・抽出
- 👤 元データのスクリーンショットを1〜2ページ見せる
- 🤖 デシジョンツリーに従い抽出方法（pdfminer/pymupdf/座標ベース）を判断し、抽出スクリプトを書いて実行
- 🤝 AI→人間：抽出結果（CSVの該当行の中身そのもの、数件分）を実物画像と並べて提示し、確認を取る（ズレていれば🤖修正→再実行を繰り返す）。「抽出できました」と結果だけ報告して終わらせない
- 🤝 カード項目（事業名／タグ／story_p1・p2・impact等）が元シートのどこから来て、どう加工されるか（転記／区分・分類／AI分類／AI要約／固定表示）を、フェーズ②の重い処理に着手する前に**項目を1つずつ名指しして**確認する（一覧表を見せるだけで済ませない。直接対応が明らかな項目も省略しない）。元シートに直接対応する専用欄がない項目（story_impact等）は、代替候補と実例（実際のシートの該当欄のテキスト）を必ず示す。全件が同じ値になる項目（root等）は、専用欄がそもそも存在しない構造的な状況の強いシグナルなので、なおさら確認が必要

### 4. フェーズ②：AI要約生成
- 🤝 AI→人間：show_*フラグ（カードのどの項目を表示するか）、personnel_cost（人件費の含み方）、enable_voting（投票機能を使うか。Falseならフェーズ④のSupabase作業が丸ごと不要になる）等、データの有無ではなく実態・好みで決めるべき項目を確認して回る
- 🤖 story_p1/p2を少数件（20件程度）で試作し、機械的な品質チェックを実行
- 🤝 AI→人間：試作結果を全文提示し、この方法で残り全件に展開してよいか確認を取る（自己評価の報告だけで済ませない）
- 🤖 残り全件をバッチ生成（300件を超える場合はOAuthトークンの途中失効を避けるため`ANTHROPIC_API_KEY`推奨）、機械的な品質チェック（年度表現・コスト表現・誤記載パターン等）を実行

### 5. フェーズ③：HTML生成
- 🤖 `build_html.py`をコピーして`LABELS`を埋め、HTMLをビルド
- 🤖 「📖 カードの説明」画面（`renderGuide()`関数）の中身を対象自治体向けに書き換える。**`LABELS`を埋めるだけでは自動的に直らない**：コピー元（北本市版）の文言・変換表が固定文字列としてそのまま残っており（例：「事務事業一覧（Excel）の『事務事業名』欄」「北本市の人口」、行政評価タグの北本市固有の変換表など）、書き換えないと事実と異なる説明が公開されてしまう。ステップ3で項目ごとに確認済みのマッピング内容をそのまま転記する（`fixed_prompt.md`フェーズ③Step4参照）
- 🤝 AI→人間：生成カードと元データを10〜20件並べて実機比較レビュー（「カードの説明」画面に他自治体名や誤った出どころが残っていないかも確認する）

### 6. フェーズ④：公開
- 👤 GitHubで新規リポジトリを作成し、完成したHTMLをルートに配置する
- 👤 投票機能を使う場合はSupabaseで新規プロジェクトを作成し、テーブル作成SQLを実行する（本ファイル「独立運用のための読み替え」参照）。この2つはアカウントを持つ本人にしかできない作業
- 🤝 AI→人間：発行されたURL・キー等を受け取り、HTML内のプレースホルダーを実値に差し替える。pushの実行を任せてもらえる場合はその旨も確認する
- **外部サービス（Supabase・GitHub等）の画面操作は、複数ステップをまとめて一度に案内しない**（2026-08-04追加・朝来市実地テストで発覚）：SupabaseやGitHubのUIは頻繁に変わり、事前に「正しいはずの手順」を決め打ちで箇条書きにすると、実際の画面と食い違って人間が迷う（朝来市で実例あり：「Project Settings→API」の案内が実際のUIと合わず、「anon public」と「Publishable key」のどちらを使うか等で質問が発生した）。**1〜2ステップずつ案内し、その都度スクリーンショットを見せてもらって実際の画面を確認してから次のステップを示す**、という対話ペースで進める。人間から「どこですか？」と聞き返される前に、AI側から次の一手を能動的に示すことで、離脱（迷って諦めてしまうこと）を防ぐ。ただし、これは「詳細な説明を省く」という意味ではない——**最終的に何をコピー・入力すればよいか（URL形式・キーの接頭辞・SQLの中身等）は明確に伝えつつ、そこへ辿り着くための画面操作は都度確認しながら進める**、という使い分け。さらに、**画面に映っている入力欄・選択肢は、重要度に関わらず全て説明する（AIが「説明不要」と判断して省略しない）**：朝来市では、スクリーンショットを見た上での案内であっても「GitHub (optional)」欄がAIの判断で説明から漏れ、人間が別途質問する場面があった。ペースを落として画面を確認していても、画面上の要素を選別して一部だけ説明する（＝AIが独自に「これは説明不要」と判断する）と同じ種類の見落としが起きる
- **独立運用では、公開ファイル名を`index.html`にリネームして404の根本原因自体をなくす**（2026-08-04追加・朝来市実地テストで発覚）：出力ファイルは元々`{自治体名ローマ字}_index.html`という名前（制作者本人の共有ハブリポジトリで複数自治体のファイルが同居するための命名規則）。独立運用では自分専用の単独リポジトリになり名前の衝突が起きないため、**GitHubにアップロードする際は`index.html`にリネームする**。こうすればGitHub Pagesの素のURL（`https://（ユーザー名）.github.io/（リポジトリ名）/`）でもそのまま開き、GitHub Pages標準の「Visit site」ボタンも正しく機能する（朝来市では旧ファイル名のままアップロードしたため、「Visit site」ボタンが404エラーになる実機不具合を確認した）。リネームしない場合は、案内・確認を常にファイル名まで含めた完全なURLで統一すること

## よくある落とし穴（fixed_prompt.mdに書かれている内容の中でも特に見落としやすい点）

- **Windowsで日本語パスを扱う場合、Pythonの実行はPowerShell経由で行う**：Git Bash経由だとcp932/UTF-8のエンコーディング不一致で、日本語を含むフォルダ名の`cd`・Python実行結果の表示が文字化け・失敗することがある。作業フォルダに日本語が含まれる場合は、最初からPowerShellでPythonを実行すること（朝来市実地テストで実際に数回のコマンド失敗が発生し、途中でPowerShellに切り替えて解決した：2026-08-04）
- **フェーズ①→③の事故防止**：抽出スクリプト（フェーズ①・②）を後から修正・再実行すると、フェーズ③でCSVに直接書き込んだ内容（story_p1/p2等）が消える。抽出スクリプトを触ったら必ずフェーズ③のパッチスクリプトも再実行すること
- **テキスト抽出の順序は信用しない**：選択式の帳票（A/B/C/D等）や複雑な表組みでは、抽出したテキストの順序が実際の値と入れ替わっていることがある。座標ベース抽出が必要かどうかは`extract_coordinate.py`のような実例と見比べて判断する
- **数値グリッド（同一指標の複数年度実績等）は必ず座標検算する**：「ラベル直前/直後のN番目の数値」という位置だけの判断は、抽出順序が視覚上の表構造と一致しない場合に破綻する
- **タグ・分類として表示する全項目のユニーク値は、抽出直後に必ず洗い出す**：法的根拠タグ・行政評価タグに限らず、`cls`（事業分類）・`outcome_eval`等、カード上でタグやフィルター値として使われる項目はすべて対象。元データの表現（ユニーク値）は自治体ごとに異なるため、決め打ちせず実際のユニーク値を一覧表示してから変換表を作る。特に「-」「ー」「−」「該当なし」等、一見値があるように見えて実は「未設定」を意味するプレースホルダー文字に注意する（`cls`列の「-」を空文字列と区別できず、政策未設定の40件が「-」という偽の政策カテゴリとして地図画面に表示された朝来市の実例あり：2026-08-04。機械的な品質チェックをすり抜け、Playwrightでの画面確認まで発見が遅れた）
- **budgetの単位変換は抽出スクリプト自体の中で完結させる**：元データの単位（円／千円／万円等）を抽出前に確認し、抽出後に別スクリプトや手作業で変換しない。再実行のたびに手動変換が必要な設計だと、変換を忘れて予算が桁違いにズレるリスクがある（朝来市で実例あり：2026-08-04）
- **実物のPDF/Excelを画像として必ず確認する**：スクリプトのコメントやdocstringだけを信じて「カードの説明」の対応表を書かない。実際の画像を見て初めて正確な文言が分かることが多い
- **`build_html.py`の「カードの説明」画面はコピー元の自治体名・変換表が残ったまま**：`LABELS`辞書のフィールドとは別に、`renderGuide()`関数の中に北本市版の固定文言（「北本市の人口」「事務事業一覧（Excel）の...」等）や変換表が直接書き込まれている。HTMLをビルドしただけでは自動的に対象自治体向けに変わらないため、ステップ5で必ず手動で書き換える
- **1件だけでなく実機比較レビューを行う**：機械的チェックが通っても、生成されたカードと元データのスクリーンショットを10〜20件並べて突き合わせる。低頻度の文字化けや、ラベルが空文字列になるバグは機械チェックでは検出できないことがある
- **Playwright等の検証スクリプトを書く前に、実際に生成されたHTML/JSを読んでセレクタ・関数名を確認する**：過去の自治体での経験（クラス名・関数名のパターン）を当てずっぽうで流用すると、外れるたびに読み直し→再実行の無駄な往復が発生する（朝来市で4回外れた実例あり：2026-08-04）。特にチェックボックス等、レンダリングのたびに要素が再描画される実装では、クリックの模倣ではなくJS関数を直接呼び出す必要がある場合もある
- **人間への確認は、自己評価の報告だけで済ませない**：「少数件を試作して品質確認しました、残り全件に進めてよいですか」のように可否だけを聞くのではなく、判断材料となる実際の生成物（サンプル全文そのもの）をチャット上に提示した上で了承を取る。**「該当ファイルの場所はこちらです」と伝えるだけでは足りない**（人間が別途ファイルを開く手間を要求することになり、結局中身を見ないまま可否だけ答えてしまう事故を防げない）。人間が中身を見ずに選択肢を選ぶだけの形にしない（朝来市で実例あり：2026-08-04。試作20件の中身を人間が一度も見ないまま本生成の可否を聞いていた）。**ただし、実際に生成ファイル（CSV等）がある場合は、そのファイル名・パスも合わせて明示する**（下妻市で実例あり：2026-08-06。チャット提示に加えて`trial_p1.csv`等のファイル名を伝えたことで、人間がファイルを直接開いて確認するという、より確実な検証方法を自発的に選べた）。チャット提示とファイル名明示は両方行い、どちらか一方で済ませない
- **マッピング表は項目を1つずつ名指しして確認を取る（一覧表を見せるだけで済ませない）**：カード項目（事業名／事業番号・担当課／施策／法定根拠タグ／行政評価タグ／提供形態タグ／目的・事業内容／決算額／人件費／成果／廃止した場合の影響／**人口**）が元シートのどこから来て、どう加工されるか（転記／区分・分類／AI分類／AI要約／固定表示）を、フェーズ①（抽出方式判定）の直後・フェーズ②の重い処理（story_p1/p2等の全件生成）に着手する前に、**項目ごとに「評価カード『事業名』は評価シート『事務事業名』でよろしいですか？」のように名指しで確認する**。直接対応が明らかな項目（事業名等）も省略しない。一覧表としてまとめて提示するだけだと、AIが「確認不要」と暗黙に判断した項目が漏れたまま流れてしまう（朝来市で実例あり：2026-08-04。法定根拠タグが447件全件「データなし」になっていたが確認のタイミングが一度もなく、人間が後から気づいた。story_impact側もHTMLが完成してから初めてマッピングの全体像を確認する形になっていた）。この一覧はビルド後の「カードの説明」画面と同内容なので、先に作った草案をそのまま再利用してよい。特に`story_impact`（廃止した場合の影響）のように元シートに対応する専用欄がなく代替の欄で代用する項目は、この段階で必ず実例（実際のシートの該当欄のテキスト）付きで確認を取る。story_impactは評価シートに専用欄がある自治体自体が少なく、代用時はAIの機械判定だけに任せず、ev区分に偏らない複数件のサンプルで人間と一緒に確認すること（`fixed_prompt.md`「story_impact 抽出方針」参照。朝来市で173件中6件の誤判定あり）。root（法定根拠タグ）のように**全件が同じ値になる**場合は、それ自体が「専用欄がそもそも存在しない」構造的な状況の強いシグナルなので、なおさら確認が必要。**さらに、マッピング項目のうちAIが後段で生成する文章（story_p1/p2等）に影響するもの（outcome_evalの変換ルール等）は、抽象的なルール説明だけでなく実際に2〜3件の生成結果を添えて提示する**（下妻市で実例あり：2026-08-06。変換ルール自体は早期に確認したが、story_p2の文体にどう表れるかは20件試作まで分からず手戻りが発生した）。**人口（population）はこの確認リストから漏れやすいので明示的に含める**（下妻市で実例あり：2026-08-06。事業名・事業番号・担当課・決算額・pdf_pageは「直接対応」として確認したが、populationは対象に含まれず確認のタイミングが一度もないまま実装されていた。実装自体は`population_latest.csv`参照という確立済み手順に正しく従っており実害はなかったが、他の項目と違い「自治体ごとに判断する要素がない固定手順」である旨を一言添えて確認すればよい）
- **populationは`population_latest.csv`（同梱）から取得する**：Web検索や元PDFの受益者数等を流用しない。詳細は`fixed_prompt.md`のLABELS定義コメント参照。朝来市では代用元の候補が2つ（PDF内の受益者数、住民基本台帳人口）出てきたが、いずれも同梱CSVの値・出典（デジタル庁 地方財政ダッシュボード）とは異なり、他自治体との出典表記の一貫性が崩れるところだった（2026-08-04）
- **座標抽出の1文字混入に注意する**：ラベル文字（見出し等）の断片1文字だけが、隣接するセル（指標名等）に混入することがある（下妻市で実例あり：2026-08-06、特定の部署のページに集中して11件発生）。座標ベースで数値・指標名を抽出する場合、抽出結果に1文字だけの異常な値がないか機械チェックすると検出しやすい
- **フェーズ④はenable_voting=Trueの場合、Supabase設定→GitHub公開の順で行う**：GitHubを先にプレースホルダーのままアップロードし、後からSupabaseを設定すると、「本物の値で再ビルド→GitHubへ再度アップロード」という手戻りが発生する（下妻市で実例あり：2026-08-06）。Supabaseのプロジェクト作成・テーブル作成・キー取得を先に終わらせてから、本物の値が入った完成版HTMLを一度だけGitHubにアップロードする
- **AI利用の開示はマップ画面（利用開始時点）に自動で入る**：商業利用規約は「モデルの出力を個人に直接提示する場合、少なくとも各セッションの開始時に開示すること」を求めている。`build_html.py`のマップ画面（`renderMap`）には常時表示の開示文が組み込まれているため、通常は何もしなくてよい。ただしrenderMapを大きく書き換える場合は、この開示文（`.ai-disclosure`のブロック）を削らないよう注意する（2026-08-06追加）

## 完了の目安

以下がすべて揃って初めて完了とみなす（fixed_prompt.mdフェーズ③末尾の品質チェックを参照）：
1. 機械的チェック（年度表現・コスト表現・センチネル値の誤記載がないか）を通過している
2. 除外ワード分類（「なじまない」等）を全件目視確認している
3. 生成カードと元データの実機比較レビューを10〜20件行っている
4. 「📖 カードの説明」画面（`renderGuide()`）が対象自治体向けに書き換わっており、コピー元（北本市版）の自治体名・変換表が残っていない
5. ご自身のGitHubリポジトリへの公開（フェーズ④、ただし「独立運用のための読み替え」節の通り新規リポジトリ・新規Supabaseプロジェクトを使用）まで完了している

## 詰まったら

無理に押し通さず、一旦立ち止まって「どのフェーズの、何が原因で詰まっているか」を整理すること。fixed_prompt.mdの該当箇所（特にフェーズ①のデシジョンツリー、フェーズ③の座標ベース抽出の判断基準）に、同じ問題への対処法が既に書かれている可能性が高い。
