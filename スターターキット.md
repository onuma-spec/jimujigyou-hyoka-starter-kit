# 事務事業優先度評価ツール 自治体追加スターターキット

このファイルはAIコーディング支援（Claude Code等）が最初に読むためのオンボーディング文書です。
人間の operator は、このファイルと下記の同梱ファイルを渡した上で「〇〇市(区町村)を追加したい」と伝えるだけで作業を開始できます。

同梱している`fixed_prompt.md`・`build_html.py`等のファイル群は、GitHub上でMITライセンス（自由に閲覧・コピー・改変・再配布可能）で公開されているものと同一内容です。ご自由に使っていただいて構いません。

## このプロジェクトの目的

各自治体が公表している事務事業評価データ（PDF/Excel）を、住民が「続行・廃止・見直し」の3択で評価し、投票として共有できる単一HTMLツールに変換する。既に7自治体（西宮市・北本市・湖西市・品川区・富田林市・聖籠町・一宮市）で稼働済み。今回はこれと同じものを、新しい自治体向けに作る。

## 権威ある手順書

**`fixed_prompt.md`（同じフォルダ内）が唯一の詳細な作業手順書です。** 標準列定義・タグ定義・4フェーズの作業フロー・デシジョンツリー・品質チェック手順など、すべてここに書かれています。このスターターキットはfixed_prompt.mdの内容を要約・重複するものではなく、**「最初に何をすべきか」と「同梱ファイルの使い方」だけを補足するもの**です。作業中に迷ったら必ずfixed_prompt.mdの該当フェーズに戻って確認してください。

## 事前準備（環境構築）

**Claude Codeへ**：作業を始める前に、まず利用者に「環境構築(Python・必要なライブラリのインストール)はご自身で済んでいますか？それともこちらで確認・インストールしましょうか？」と確認してください。「自分で済んでいる」という返答であれば下記が整っていることを前提に次のステップへ進み、「任せる」という返答であればバージョン確認・インストールコマンドの実行から一緒に進めてください。

最終的に使える状態にしておく必要があるもの：

- **VSCode＋Claude Code**（またはコード実行可能な同等のAIコーディング支援）：チャットで依頼するだけでなく、実際にPythonスクリプトを書いて実行できることが必須（PDFの読み取り・座標ベース抽出は試行錯誤が必要なため、コード実行できないチャットAIだけでは代用できません）
- **Python 3系**：インストール済みであること
- **必要なライブラリ**：以下をコマンドラインで一度インストールする
  ```
  pip install pdfminer.six pymupdf openpyxl pandas
  ```
  - `pdfminer.six`：PDFからのテキスト抽出（`extract_simple.py`・`coord_extract_utils.py`で使用）
  - `pymupdf`（fitz）：文字化けするPDFの代替抽出、および数値グリッドの座標ベース抽出（`coord_extract_utils.py`内で使用）
  - `openpyxl`：元データがExcelの場合の読み込み
  - `pandas`：Excelデータの集計・整形

## 独立運用のための読み替え（重要）

fixed_prompt.mdは、このツールの制作者本人の環境・アカウント向けに書かれています。**今回は制作者から完全に独立した別プロジェクトとして運用するため**、以下の4点は必ず読み替えてください。

1. **作業ディレクトリ**：fixed_prompt.md冒頭の「作業ディレクトリ：`C:\Users\onuma\Desktop\AIの作業場\小さな政府\07_事務事業評価\`」は制作者個人のPCパスです。**ご自身が用意した作業フォルダに読み替えてください**。文中の「01_元データ\」「02_抽出スクリプト\」等のサブフォルダ名も、ご自身の作業フォルダを起点に解釈してください（同じ分割を再現しても構いませんし、フラットに1フォルダにまとめても構いません）
2. **同梱ファイルの配置**：今回共有している`extract_simple.py`・`extract_coordinate.py`・`coord_extract_utils.py`・`build_html.py`・`story_gen_prompt.md`は、元のプロジェクトでは別々のサブフォルダに分かれていますが、**今回はこのフォルダに直接まとめて共有しています**。fixed_prompt.md内でサブフォルダ付きのパス表記が出てきても、実際にはこのフォルダ直下にあるものとして読み替えてください
3. **ファイル名の対応**：fixed_prompt.mdは制作者本人の内部ファイル名（自治体名がそのまま付いたもの）で書かれていますが、このキットでは役割を直接表す汎用的な名前に変更しています。fixed_prompt.md中に以下の名前が出てきたら、右側の同梱ファイルに読み替えてください。
   | fixed_prompt.md中の表記 | このキットでの実際のファイル名 |
   |---|---|
   | `build_kitamoto_v3.py`（またはbuild_{自治体名}_v3.py） | `build_html.py` |
   | `extract_kitamoto_v2.py` | `extract_simple.py` |
   | `extract_tondabayashi_v2.py` | `extract_coordinate.py` |
4. **公開先（フェーズ④）は完全に新規で用意する**：fixed_prompt.mdは制作者本人のGitHubリポジトリ（`jimujigyou-hyoka`）・Supabaseプロジェクトへの公開手順が書かれていますが、**これらには一切アクセスせず、以下を新規に用意してください**
   - **GitHub**：fixed_prompt.md冒頭の「ハブリポジトリ：`https://github.com/onuma-spec/jimujigyou-hyoka`」、およびフェーズ④の「GitHubアップロード先：`jimujigyou-hyoka` リポジトリのルートに配置する」は、いずれも制作者本人のリポジトリです。**これらは無視し**、ご自身のアカウントで新規リポジトリを作成してそこにHTMLをアップロードしてください（既存の`jimujigyou-hyoka`ハブへの登録は行わない＝完全に独立したツールとして運用する）
   - **Supabase**：[supabase.com](https://supabase.com)で新規に無料プロジェクトを作成し、`build_html.py`内の`SUPABASE_URL`・`SUPABASE_KEY`をご自身のプロジェクトの値に書き換える。**注意**：fixed_prompt.mdフェーズ③「Supabase定数」には「URL・keyはbuild_kitamoto_v3.pyからそのままコピー」「Supabaseプロジェクトは全自治体で共有、新自治体追加時のSupabase側作業は不要」と書かれていますが、これは制作者本人の環境向けの記述であり、**今回は無視して必ず新規プロジェクトの値に置き換えてください**。テーブルも以下のSQLで新規作成する必要がある（SupabaseのSQL Editorで実行）：
     ```sql
     CREATE TABLE ratings (
       id              bigserial PRIMARY KEY,
       municipality_id text        NOT NULL,
       event_no        text        NOT NULL,
       rating          text        NOT NULL,
       submitted_at    timestamptz NOT NULL DEFAULT now()
     );

     ALTER TABLE ratings ENABLE ROW LEVEL SECURITY;
     CREATE POLICY "anyone can insert" ON ratings FOR INSERT TO anon WITH CHECK (true);
     CREATE POLICY "anyone can select" ON ratings FOR SELECT TO anon USING (true);
     GRANT SELECT, INSERT ON TABLE public.ratings TO anon;
     GRANT USAGE, SELECT ON SEQUENCE public.ratings_id_seq TO anon;
     ```
     （UPDATE・DELETEはanonに許可しない＝運用するご本人だけがSQL Editorから行う設計。個人情報は一切収集しないテーブル設計なので、この点は変更せずそのまま踏襲することを推奨）

## 同梱ファイルと使い方

| ファイル | 役割 |
|---|---|
| `fixed_prompt.md` | 権威ある作業手順書（上記参照） |
| `build_html.py` | ビルド用Pythonテンプレート（フェーズ③・HTML生成）。全自治体共通のHTML/CSS/JSロジックを持つ。**これをコピーして`build_{自治体名}.py`を作り、中の`LABELS`辞書だけを書き換える**。ロジック部分は変更不要。カードの各セクション（事業番号・担当課／タグ／目的・事業内容／決算額・人件費／成果／廃止した場合の影響）は`LABELS.show_*`フラグで個別に表示・非表示を選べる（データの有無ではなく制作者の好みで決めてよい。詳細は`LABELS`辞書内のコメント参照） |
| `extract_simple.py` | PDFがシンプルな表形式で、pdfminerでそのままテキストが正しい順序で読める場合の標準的な抽出スクリプト例（フェーズ①） |
| `extract_coordinate.py` | PDFのレイアウトが崩れている（選択式の帳票、座標がずれた表組み等）場合の応用例（フェーズ①）。`coord_extract_utils.py`を使った座標ベース抽出の実装例 |
| `coord_extract_utils.py` | 座標ベース抽出が必要な場合に使う汎用関数群（`find_stamps`/`find_label_runs`等） |
| `story_gen_prompt.md` | 評価カードの要約文（目的・事業内容／成果／廃止した場合の影響）をAIに生成させる際のプロンプトテンプレート（フェーズ②）。**必ずこの最新版をそのまま使うこと**（古いコピーは既知のバグを再発させる） |

完成品の見本は、実際に公開されている[北本市版](https://onuma-spec.github.io/jimujigyou-hyoka/kitamoto_index.html)を参照してください。

## 最初にやること

1. 対象自治体名を確認する
2. **元データ(事務事業評価のPDF、またはExcel)がまだ手元にない場合**：まずWeb検索で該当自治体の「事務事業評価」公表ページを探す。公表形式・場所は自治体ごとにバラバラで、複数年度分が混在していたり、そもそも公表されていない場合もある。**見つけた候補は必ず利用者に提示し、これで間違いないか(正しい自治体・最新年度か等)確認を取ってから次に進む**。AIが見つけたものを確認なしに使わない
3. 元データのスクリーンショットを1〜2ページ分見せてもらう（PDFなら冒頭ページ、Excelなら先頭シート）
4. `fixed_prompt.md`フェーズ①のデシジョンツリーに従い、抽出方法（pdfminer / pymupdf / 座標ベース）を判断する
5. 以降はフェーズ②〜④の順に進める

## よくある落とし穴（fixed_prompt.mdに書かれている内容の中でも特に見落としやすい点）

- **フェーズ①→③の事故防止**：抽出スクリプト（フェーズ①・②）を後から修正・再実行すると、フェーズ③でCSVに直接書き込んだ内容（story_p1/p2等）が消える。抽出スクリプトを触ったら必ずフェーズ③のパッチスクリプトも再実行すること
- **テキスト抽出の順序は信用しない**：選択式の帳票（A/B/C/D等）や複雑な表組みでは、抽出したテキストの順序が実際の値と入れ替わっていることがある。座標ベース抽出が必要かどうかは`extract_coordinate.py`のような実例と見比べて判断する
- **数値グリッド（同一指標の複数年度実績等）は必ず座標検算する**：「ラベル直前/直後のN番目の数値」という位置だけの判断は、抽出順序が視覚上の表構造と一致しない場合に破綻する
- **タグ変換表は自治体ごとに必ず確認する**：法的根拠タグ・行政評価タグの元データ表現（ユニーク値）は自治体ごとに異なる。決め打ちせず、実際のユニーク値を洗い出してから変換表を作る
- **実物のPDF/Excelを画像として必ず確認する**：スクリプトのコメントやdocstringだけを信じて「カードの説明」の対応表を書かない。実際の画像を見て初めて正確な文言が分かることが多い
- **1件だけでなく実機比較レビューを行う**：機械的チェックが通っても、生成されたカードと元データのスクリーンショットを10〜20件並べて突き合わせる。低頻度の文字化けや、ラベルが空文字列になるバグは機械チェックでは検出できないことがある

## 完了の目安

以下がすべて揃って初めて完了とみなす（fixed_prompt.mdフェーズ③末尾の品質チェックを参照）：
1. 機械的チェック（年度表現・コスト表現・センチネル値の誤記載がないか）を通過している
2. 除外ワード分類（「なじまない」等）を全件目視確認している
3. 生成カードと元データの実機比較レビューを10〜20件行っている
4. ご自身のGitHubリポジトリへの公開（フェーズ④、ただし「独立運用のための読み替え」節の通り新規リポジトリ・新規Supabaseプロジェクトを使用）まで完了している

## 詰まったら

無理に押し通さず、一旦立ち止まって「どのフェーズの、何が原因で詰まっているか」を整理すること。fixed_prompt.mdの該当箇所（特にフェーズ①のデシジョンツリー、フェーズ③の座標ベース抽出の判断基準）に、同じ問題への対処法が既に書かれている可能性が高い。
