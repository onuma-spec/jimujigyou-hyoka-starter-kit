# 事務事業評価ツール 自治体追加 固定プロンプト

## プロジェクト概要
各自治体の事務事業評価データを住民が3段階評価（続行・廃止・見直し）できる静的HTMLツール。
自治体ごとに `XX_index.html` を生成し、ハブ（jimujigyou-hyoka）経由または直接URLで公開する。

## 環境設定（作業開始前に必ずどちらか選ぶ）

本文中に出てくる作業ディレクトリ・GitHub公開先・Supabase URL/KEY・population参照元は、すべてここに集約されている。本文の該当箇所では値を繰り返さず「環境設定を参照」とだけ書くので、実行時はここで選んだプリセットの値を当てはめること。

**`ANTHROPIC_API_KEY`のような真のシークレットはここには含めない**（環境変数として各自で設定するのみ。値をこの文書や設定ブロックに書くことは絶対にしない。詳細は`03_パッチ済みデータ\story_gen_prompt.md`参照）。以下の4項目はいずれも公開前提の識別子（Supabase KEYは"publishable key"＝クライアント側に公開される前提の値で、保護はRLSで行う設計）。

| 項目 | ①ハブ運用（制作者本人） | ②独立運用（スターターキット） |
|---|---|---|
| 作業ディレクトリ | 制作者本人の作業フォルダ | スターターキット.mdのステップ0でclone/展開した場所 |
| GitHub公開先 | `https://github.com/onuma-spec/jimujigyou-hyoka`（ルートに配置＋ハブ登録あり） | 自分のアカウントで新規作成したリポジトリ（ハブ登録は行わない） |
| Supabase URL | `https://unkhamvtfathmoxmikmp.supabase.co` | 新規プロジェクトのURL（`enable_voting=False`なら不要） |
| Supabase KEY | `sb_publishable_UArsQ1bcdira6J91O8kXjA_nGJ6MGtz` | 新規プロジェクトのpublishable key（同上） |
| population参照元 | `02_全国版\元データ\地方財政（市町村ごと）のデータ\finance_data_table_master.csv` | 同梱の`population_latest.csv` |

②を選ぶ場合のSupabase新規プロジェクト作成手順（テーブル作成SQL含む）は`スターターキット.md`「独立運用のための読み替え」節を参照。

## フォルダ構成

2026-07-08再整理・データが①元データ→②抽出→③パッチ→④HTML生成の順に流れる保管場所：
- 直下：最終成果物のみ（`index.html`＋各自治体の`*_index.html`）
- `01_元データ\`：①各自治体の生PDF/Excel（ダウンロードそのまま）
- `02_抽出スクリプト\`：②元データ→マスターCSVへの抽出・変換スクリプト（`extract_*.py`等）、共通ユーティリティ（`coord_extract_utils.py`）
- `03_パッチ済みデータ\`：③マスターCSVへ後から書き込むパッチスクリプト（`gen_story_*.py`・`story_gen_prompt.md`等）＋ **全自治体の最終マスターCSV**（②が作り③が上書きした後の状態がここに集約される）
- `04_本番HTML生成\`：④マスターCSV・分類JSON（`{name}_classified_all.json`・`{name}_revised.json`）からHTMLを生成するスクリプト（`build_{name}_v3.py`）＋ビルド直後のHTML出力
- `06_スターターキットリポジトリ\`：本ドキュメント（`fixed_prompt.md`）＋スターターキット一式（GitHub `jimujigyou-hyoka-starter-kit` と同期するgit作業ツリー、唯一の編集場所）
- `旧v2バックアップ\`：v3移行前（〜2026-07-06）のビルドスクリプト・生成HTML（参照専用、通常は触らない）

※ この①〜④はフォルダ内でデータが保管される順序であり、下記「作業フロー（4フェーズ）」のフェーズ①〜④とは1対1で対応しない（同じ丸数字だが別の numbering）。例：フェーズ①「取得」の作業は`01_元データ`と`02_抽出スクリプト`の両方にまたがり、フェーズ③「生成」の作業は`04_本番HTML生成`フォルダで行う。フォルダ番号とフェーズ番号を混同しないこと。

**②→③→④の重要な注意**：②（抽出スクリプト）を再実行すると、③でCSVに直接書き込んだ内容（story_p1/p2等）が消える。②を触ったら必ず③のパッチスクリプトも再実行すること（2026-07-08に実際の事故と復旧あり）。各`build_{name}_v3.py`冒頭のdocstringに自治体ごとのパイプライン詳細を記載済み。

---

## 標準定義

### master_data.csv 標準列

| 列名 | 内容 | 必須 |
|---|---|---|
| no | 事業番号 | ✅ |
| name | 事務事業名 | ✅ |
| bureau | 局・部（上位部署） | ✅ |
| dept | 担当課 | ✅ |
| budget | 当年度予算（千円） | ✅ |
| root | 法的根拠タグ | ✅ |
| ev | 行政評価・方向性 | ✅ |
| overview | 事業概要（最大400字） | ✅ |
| pdf_page | PDFページ番号 | ✅ |
| cls | 事業分類・政策 | -（ない場合の対応はフェーズ③参照。生の空欄のまま表示しないこと） |
| story_p1 | 天秤①完成文（AI生成） | ✅ |
| story_p2 | 天秤②完成文（AI生成） | ✅ |
| story_impact | 廃止影響（Action理由から抽出） | ✅（ない場合は空白） |
| output_name | 活動指標名 | ✅ |
| output_val | 活動指標実績 | ✅ |
| outcome_name | 成果指標名 | ✅（ない場合は空白） |
| outcome_val | 成果指標実績 | ✅（ない場合は空白） |
| outcome_eval | 成果指標評価（A/B/C） | ✅（ない場合は空白） |

### タグ定義

**法的根拠タグ（root列）**
- `法定義務あり`：法定受託・法令義務・条例義務
- `法定義務なし`：法令・条例根拠なし（裁量的事業）
- `判断できない`：法的根拠が不明または判定不能（内部管理・投資的事業・必需性のみ記載など）
- `データなし`：評価シートに法的根拠フィールド自体が存在しない、またはデータが提供されていない
※ root のユニーク値→タグ変換は自治体ごとに確認する（変換表テンプレート参照）
※ この4タグはv2（聖籠町以降）の定義。v1既存ツールは変更なし（裁量/評価対象外を維持）

**行政評価タグ（ev列）**
表示順：`拡大` / `現状継続` / `改善（手段変更）` / `縮小` / `廃止方向` / `完了`
- 拡大：事業を積極的に広げる
- 現状継続：現状を維持する
- 改善（手段変更）：手段・民間活用・広域連携等で効率化（他事業との整理・統合もここに含む）
- 縮小：事業規模を縮小する
- 廃止方向：休止・廃止・事業終了（問題があっての終了）
- 完了：計画通り目的を達成しての終了（2026-07-01・西宮市で追加。廃止方向とは異なり前向きな終了）
※ フィルタードロップダウンはこの順で表示。カード表示色は拡大=緑系・廃止方向=赤系・完了=紫系（中立色）。
※ ev のユニーク値→タグ変換は自治体ごとに確認する（変換表テンプレート参照）。「事業を完了した」という選択肢がない自治体では発生しない（聖籠町・湖西市で確認済み・0件）

**天秤③状態（自動判定）**
- story_impact が文字列（`''` でも `'判断できない'` でもない） → テンプレート表示
  「この事業がなくなった場合、{story_impact}という問題が起きると想定されています。」
- story_impact が `''` または `'判断できない'` → フォールバック表示
  「廃止した場合の影響が評価シートに記載されていません。」

**story_impact 抽出方針**
- 廃止影響が明確に読み取れる → 抽出テキスト（文末「。」まで）
- 読み取れない（継続・改善の根拠のみ記載）→ `'判断できない'`
- **経験則（2026-08-04・朝来市で発覚）**：評価シートに「廃止した場合の影響」を専用に問う欄がある自治体は少なく、これまでの実績上ほとんどの自治体で大半または全件が`'判断できない'`になる（北本市・朝来市とも同様）。専用欄がなく代替の欄（「二次評価の評価理由」等）で代用する場合、AIが機械的に「読み取れる/読み取れない」を判定すると、前向きな「今後の方針」を誤って廃止影響として抽出する誤判定が起きうる（朝来市で173件中6件発生、`ev=廃止方向`の7件だけを検算して発覚。廃止方向以外の行は未検算だった）。**代替の欄を使う場合は必ずフェーズ①のマッピング確認（下記「注意事項」参照）で、ev区分に偏らず（拡大〜廃止方向まで）複数件の実際の抽出候補テキストを人間に見せ、本当に「廃止した場合の影響」と呼べる内容かどうかを、全件適用の前に一緒に確認すること**

### ストーリーテンプレート

```
【第1段落】（LABELS.show_desc が True の自治体のみ表示）
story_p1（AI生成・そのまま表示）
→ 生成方法は 03_パッチ済みデータ\story_gen_prompt.md を参照

【第2段落】（LABELS.show_outcome が True の自治体のみ表示）
story_p2（AI生成・そのまま表示）
→ 生成方法は 03_パッチ済みデータ\story_gen_prompt.md を参照
→ 含める要素：活動指標（output_name・output_val）・成果指標（outcome_name・outcome_val・outcome_eval）
→ コスト情報・年度表現は含めない（03_パッチ済みデータ\story_gen_prompt.md 参照）
→ outcome_name が「データなし」または空白の場合は「成果を測る指標が設定されていません。」のみ出力

【第3段落】（LABELS.show_impact が True の自治体のみ表示）
この事業がなくなった場合、{story_impact}という問題が起きると想定されています。
※ story_impact が空白の場合：廃止した場合の影響が評価シートに記載されていません。
```

事業番号・担当課（LABELS.show_emeta）、タグ行（LABELS.show_tags）、決算額・人件費表示（LABELS.show_budget、
セット扱い）も同じ仕組みで表示・非表示を選べる。

※ show_*の判断基準は上記LABELSテンプレート直後の説明を参照。
　show_desc/show_outcome/show_impactをFalseにする場合、AI注記文（「※目的・事業内容、成果、廃止した場合の影響の
　説明文は…」）も表示中のセクションのみを列挙する形に自動で変わる（ビルドテンプレート側に実装済み・コピー時に自動継承）。

### フィルター軸

| 軸 | 列 | 備考 |
|---|---|---|
| 行政の評価結果 | ev | |
| 住民の評価結果 | LocalStorage | |
| 法的根拠 | root | |
| 事業分類・政策 | cls | 空白の自治体も常にフィルター表示 |

---

## 作業フロー（4フェーズ）

### フェーズ① 取得

**デシジョンツリー（抽出ツール選定）**

```
① 元データのスクリーンショットを共有する
   （PDFは1〜2ページ目、Excelは先頭シート）

② 元データの形式は？
   ├── Excel のみ
   │   └── openpyxl / pandas で取得
   ├── PDF のみ
   │   └── pdfminer LTChar でテスト抽出
   │       ├── 日本語が正常に読める → pdfminer LTChar を使用
   │       │   └── ③ の並び順チェックへ
   │       └── 文字化けする → pymupdf（fitz）を試す
   │           ├── 正常に読める → pymupdf を使用
   │           └── どちらも文字化け → 別途調査
   └── PDF ＋ Excel 混在
       ├── Excel → 基本情報（事業名・担当課・概要等）
       └── PDF  → 補完情報（予算・根拠法令・評価スコア等）
           ※ PDF の抽出は「PDF のみ」と同じ手順で確認

③ extract_text()の文字列順序は信頼できるか？（富田林市で必須になった追加チェック）
   選択式の帳票（A/B/C/D等）・複雑な表組みを含むページで、抽出テキストの
   選択肢定義と実際の値が入れ替わっていないか目視確認する。
   ├── 順序が正しい → 通常どおり正規表現ベースで抽出
   └── 順序が乱れている（LTTextBoxがほぼ空でLTFigure内にLTCharがフラットに
       並ぶ構造等）→ 座標ベース抽出に切り替える（`coord_extract_utils.py`参照）
       ├── 選択式グリッド（A/B/C/D等）が対象 → 「回答スタンプ」方式
       │   富田林市の帳票は「選択された回答だけが太字・大サイズ(7pt以上)で
       │   孤立した単一文字として描画される」という特徴があった。対象PDFで
       │   本文サイズとスタンプ候補サイズの分布を確認し、閾値を調整して使う
       │   （`find_stamps()`。Boldフラグは非Boldの場合があり使えないので注意）
       └── ラベル＋値のペア（指標名等）が対象 → 「行帯＋列」方式
           ラベルと値が同じ行にない（長い値が2行に折り返りラベルが中間に
           挟まる、ラベルと値のy中心が数pt程度ずれる等）帳票で、ラベル自体の
           正確な座標を先に特定し、隣接ラベルとの中間点を行の境界とすることで
           取りこぼしを防ぐ（`find_label_runs()` / `extract_by_label_column()`）

④ 数値テーブル（同一指標の複数年度実績等が横に並ぶグリッド）を抽出する場合
   （湖西市で必須になった追加チェック・成果指標のR5実績/R6目標/R6実績/R7目標等）
   「ラベル直前/直後のN番目の数値」という位置ヒューリスティックは、pymupdf
   `get_text("text")`の行順序が視覚上の表構造と一致しない場合に破綻する
   （湖西市では19/142件で欠落・隣接列の誤取得が発生。ラベル自体が消失する
   ケースすらあった）。
   ├── 数件をget_text("dict")で座標検算し、値が正しく取れているか確認する
   │   （検算せずに位置ヒューリスティックだけで済ませない）
   └── ズレがある場合 → 座標グリッド方式に切り替える。列見出し（例：
       「R6 2024」＋「実績」等）のx座標を特定し、その列のy方向十数〜数十pt
       下にある数値スパンを値として採用する（詳細：[[project-gyosei-hyoka]]
       内「湖西市 outcome_val抽出バグ修正」節のコード例を参照）
```

**テスト抽出コマンド（pdfminer LTChar）**

```python
from pdfminer.high_level import extract_pages
from pdfminer.layout import LTTextBox, LTChar
import sys; sys.stdout.reconfigure(encoding='utf-8')
path = r'対象PDFのパス'
page = list(extract_pages(path, page_numbers=[0]))[0]
chars = [ch.get_text() for e in page if isinstance(e, LTTextBox)
         for l in e for ch in l if isinstance(ch, LTChar)]
print(''.join(chars)[:500])
```

**座標ベース抽出が必要な場合**：`07_事務事業評価\02_抽出スクリプト\coord_extract_utils.py` に汎用関数
（`find_stamps` / `find_label_runs` / `find_section_bound` / `extract_by_label_column` /
`reconstruct_lines`）がまとまっている。`extract_tondabayashi_v2.py` が使用例。
座標値（サイズ閾値・x範囲・y許容誤差）は自治体のPDFレイアウトごとに異なるため、
対象PDFに合わせて都度調整が必要（デフォルト値をそのまま信用しないこと）。
pymupdf(fitz)を使う自治体で数値グリッド（同一指標の複数年度実績等）を抽出する場合は、
同ファイル末尾の`get_page_spans_pymupdf` / `extract_grid_value_pymupdf`を使う。

---

### フェーズ② 変換

**マッピングテーブル（空欄を埋める）**

| 標準列 | ソースフィールド名 | 変換方法 |
|---|---|---|
| no | | そのまま |
| name | | そのまま |
| bureau | | そのまま |
| dept | | そのまま |
| budget | | 千円単位（元データの単位（円／千円／万円等）を抽出前に確認し、**抽出スクリプト自体の中で**変換まで完結させる。抽出後に別スクリプトで手動変換しない） |
| root | | 下記タグ変換表参照 |
| ev | | 下記ev変換表参照 |
| overview | | 最大400字 |
| pdf_page | | 整数 |
| cls | | そのまま（空欄・プレースホルダー文字「-」「ー」「−」等は空白として扱う。ユニーク値を必ず確認すること） |
| story_p1 | | 03_パッチ済みデータ\story_gen_prompt.md でAI生成 |
| story_p2 | | 03_パッチ済みデータ\story_gen_prompt.md でAI生成 |
| story_impact | | Action理由から抽出（ない場合は空白） |
| output_name | | そのまま |
| output_val | | そのまま |
| outcome_name | | そのまま（ない場合は空白） |
| outcome_val | | そのまま（ない場合は空白） |
| outcome_eval | | A/B/C（ない場合は空白） |

**法的根拠タグ変換表（空欄を埋める）**

| ソースの値 | 変換後タグ |
|---|---|
| （ユニーク値を列挙） | 法定義務あり／裁量／評価対象外 |

**行政評価変換表（空欄を埋める）**

| ソースの値 | 変換後 |
|---|---|
| （ユニーク値を列挙） | 現状継続／改善（拡大）／改善（手段変更）／縮小／廃止方向 |

**Y/Nチェックリスト**

- [ ] 事業分類・政策フィールドは存在するか（存在する場合：フィールド名＝　　　）
- [ ] 評価文書（PDF）は1本か複数か（複数の場合：部署→URL対応表が必要）
- [ ] タグ・分類・フィルターとして表示する全項目（root／ev／cls／outcome_eval等）のユニーク値を、抽出直後に一覧表示して確認したか。「-」「ー」「−」「該当なし」等、一見値があるように見えて実は「未設定」を意味するプレースホルダー文字が、空欄と区別されずに紛れ込んでいないか（2026-08-04・朝来市で発覚：cls列の「-」が空文字列と区別されず、政策未設定の40件が「-」という偽の政策カテゴリとして地図画面に表示された。機械的品質チェックをすり抜け、Playwrightでの画面確認まで発見が遅れた）
- [ ] budgetの単位変換（円→千円等）は抽出スクリプト自体の中で完結しているか。抽出後に別スクリプト・手作業で変換していないか（2026-08-04・朝来市で発覚：抽出スクリプトを再実行するたびに別途手動で円→千円変換を行っており、変換を忘れると予算が1000倍ズレるリスクを抱えていた）

---

### フェーズ③ 生成（v3設計・2026-07-08更新）

v3では「支援対象×提供形態」13分類（ptype）の付与が必須ステップとして加わっている。
以下の順で進める：

**Step 1：13分類（ptype）のAI分類**

各事業のno/name/overviewを50件程度ずつバッチ分割し、13分類定義（詳細は既存自治体の
`04_本番HTML生成\build_kitamoto_v3.py`内`PTYPE_LABEL`・`PTYPE_TIER`定義を参照）に従って
サブエージェントで並列分類する。
- 主たる提供形態を特定できる → 具体的分類（1〜11のいずれか）
- 複数形態が並列で主従関係がない → 13（複数の提供形態が混在）
- 情報不足でよく分からない → 12（情報不足で分類できない）
結果を`{name}_classified_all.json`として`04_本番HTML生成\`に保存する。
G(12)・13(混在)の質的レビュー（丸数字・箇条書き等を含む事業の見逃しチェック）を行い、
再判定結果を`{name}_revised.json`として同フォルダに保存する（既存分のフォーマットを参照）。

**Step 2：ベーステンプレートのコピー**

`04_本番HTML生成\build_kitamoto_v3.py` をコピーして `build_{自治体ローマ字}_v3.py` を作成する
（北本市版が全自治体共通のコアロジック・CSS・JSを持つ標準テンプレート）。
変更箇所は下記LABELS・Supabase定数・データソースパス（`02_抽出スクリプト`で作った
マスターCSVを`03_パッチ済みデータ`から読み込むよう変更）のみ。
HTML/CSS/JS部分（ドリルダウン表の列幅・提供形態のプレーンテキスト表示・全事務事業CSV出力機能等）は
標準化済みのため変更不要。**ただし「📖 カードの説明」画面（`renderGuide()`関数、下記Step4参照）は例外**：
関数の骨格はコピーで済むが、中の説明文・変換表はコピー元自治体の内容が固定文字列で残っているため、
自治体ごとに必ず書き換える。

**LABELSテンプレート（空欄を埋める）**

```python
LABELS = {
    # 列ラベル
    'bureau':            '',   # 局/部の列ラベル（例：'局'、'担当部'）
    'dept':              '',   # 担当課の列ラベル（例：'担当課'、'主管課'）
    'cls':               '',   # 事業分類/政策のラベル（例：'施策'。cls元データが無い場合の対応は下記参照）
    'budget_col':        '',   # 当年度予算の列ラベル（例：'R7当初予算(千円)'）
    'no_col':            '',   # 事業番号の列ラベル（例：'事業番号'）

    # 画面タイトル
    'tool_title':        '',   # ブラウザタブ・h1
    'card_title':        '',   # カード評価画面の小見出し
    'summary_title':     '',   # サマリー画面のh2

    # 出力・出典
    'output_file':       '',   # 出力ファイル名（例：'seiro_v3_index.html'）
    'csv_filename':      '',   # 評価結果CSV出力ファイル名（全件CSVは別途`{muni}_事務事業データ一覧.csv`固定）
    'source_url':        '',   # データ出典URL

    # 計算・セッション
    'population':        0,    # 住民人口（カードの一人あたりコスト計算用）
    # ↑ Web検索ではなく、冒頭「環境設定」のpopulation参照元（出典：デジタル庁 地方財政ダッシュボード
    #   https://www.digital.go.jp/resources/japandashboard/municipal-finance）から、該当市区町村・
    #   最新年度（2024年度＝令和6年度）の人口数_人列を使うこと。決算値（budget）と時点を揃えるため。
    #   北本市はWeb検索の別時点推計を誤用し訂正した実例あり（2026-07-02）。
    'personnel_cost':    '',   # 決算額に人件費が含まれるか（下記コメント参照）
    # ↑ 固定の3択（含む/含まない/確認取れず）に無理に当てはめない（2026-08-04追加・朝来市で発覚：
    #   会計年度任用職員費は事業費に含み正職員給与費のみ別会計、という部分的な状態を3択に押し込めようとして
    #   「確認取れず」に流れかけた）。評価シート・給与費明細書等を確認し、実態をそのまま日本語で記述すること
    #   （例：'会計年度任用職員費を含む（正職員人件費は別会計のため含まない）'）。単純に「含む」「含まない」と
    #   言い切れる自治体はそのまま書いてよい。評価シートから実態がどうしても特定できない場合のみ「確認取れず」とする。
    'local_storage_key': '',   # LocalStorageキー（例：'seiro6_v3_ratings'）
    'municipality_id':   '',   # Supabase識別子（命名規則：{ローマ字}_{年度} 例：'seiro_r6'）

    # PDF直接リンク（単一PDF / 複数PDFで排他使用）
    'pdf_url':           '',   # 単一PDFの場合：ベースURL
    'bureau_to_url':     {},   # 複数PDFの場合：部署名→PDF URL対応表
    # ※ 単一PDF → bureau_to_url は空dict
    # ※ 複数PDF → pdf_url は空文字

    # カードのセクション表示可否（下記「show_*の判断基準」参照）
    'show_emeta':        True,   # 事業番号・担当課を表示するか
    'show_tags':         True,   # タグ行（法定根拠／行政評価／提供形態）を表示するか
    'show_desc':         True,   # 目的・事業内容（第1段落）を表示するか
    'show_budget':       True,   # 決算額・人件費表示（セット）を表示するか
    'show_outcome':      True,   # 成果セクション（第2段落）を表示するか
    'show_impact':       True,   # 廃止した場合の影響セクション（第3段落）を表示するか
    'enable_voting':     True,   # 投票機能（Supabase連携）を使うか（下記参照）
}
```

**show_*は、データの有無ではなく利用者（本ツールの制作者）の好みで決める項目**：
評価シートに各項目のデータが構造的に存在するかどうかに関わらず、住民に見せるカードの情報量は
自治体によって好みが分かれうる（項目が多いほど認知負荷が上がる）。AIコーディング支援は、これらの値を
**データの有無から自動判定せず、必ず利用者に「（項目名）をカードに表示しますか？」と確認を取り、
その回答に沿って`True`/`False`を設定すること**（データが存在する場合でも、利用者が「不要」と
判断すれば`False`にしてよい）。`show_budget`は決算額・人件費表示をセットで扱う（片方だけを
非表示にする設計にはしない）。
`show_outcome`/`show_impact`を`False`にする場合、`story_p2`（成果）または`story_impact`（廃止した場合の影響）の
AI生成・抽出フェーズ自体を省略してよい（表示しないデータを生成する必要はないため）。ただし省略する前に、
この判断も利用者に一言確認すること（後から「やはり表示したい」となった場合、生成をやり直す手間が発生するため）。

**`enable_voting`（2026-08-04追加・朝来市実地テストで発覚）**：投票機能（Supabaseへの投票送信・みんなの評価の集計取得）を使うかどうかも、show_*と同じタイミング（フェーズ②着手前）で利用者に確認する。`False`の場合、投票ボタン・投票結果の自動取得・投票CSVダウンロードが無効化される一方、個人のLocalStorage評価・全事務事業データCSVダウンロードは引き続き機能する。**`False`を選んだ場合、フェーズ④のSupabase側の作業（新規プロジェクト作成・SQL実行・URLとキーの置き換え）は丸ごと不要になる**（GitHubへのアップロードのみで完結する）。
デフォルトは`True`（これまでの全自治体と同じ挙動）。`True`のまま独立運用する場合、フェーズ④で必ず新規Supabaseプロジェクトの値に置き換えること（`build_html.py`のデフォルト値は制作者本人の稼働中プロジェクトを指しており、置き換えずに公開すると投票データが制作者の本番環境に混入する）。朝来市ではこのフラグ自体が存在せず、代わりにプレースホルダー文字列（`YOUR-PROJECT-REF`等）が未置換のまま残り、投票画面が名前解決エラーで機能しない状態になっていた。

**cls（施策）元データが無い自治体への対応**（聖籠町の実例を踏襲）：
1. 担当課をcls軸として代替する簡易対応（旧・暫定）
2. 総合計画等の政策体系資料と事業名を照合する本格対応（推奨。聖籠町では第5次総合計画の
   「施策の方向」56項目と224事業を照合し、マッチしない事業は「内部管理・その他」に分類した。
   手順は`project_gyosei_hyoka_cls_review.md`メモリを参照）
いずれの場合も、cls値が空欄の事業は`'施策なし'`等、自治体の文脈に合った文言で埋めること
（生の`'データなし'`のまま表示しない。湖西市・品川区の実例を参照）。

**Supabase定数（build_kitamoto_v3.py からコピー・municipality_id のみ変更）**

```python
# Supabase URL・KEYは冒頭「環境設定」で選んだプリセットの値を使う
SUPABASE_URL    = '（環境設定のSupabase URL）'
SUPABASE_KEY    = '（環境設定のSupabase KEY）'
MUNICIPALITY_ID = LABELS['municipality_id']  # 自治体ごとに異なる唯一の変更箇所
```

※ ①ハブ運用の場合、Supabaseプロジェクトは全自治体で共有。新自治体追加時のSupabase側作業は不要（環境設定の値をそのままコピーするだけでよい）。②独立運用の場合は必ず新規プロジェクトの値に置き換える（詳細は`スターターキット.md`参照）。

**Step 3：ビルド・検証**

`04_本番HTML生成\`内で`python build_{自治体ローマ字}_v3.py`を実行しHTML生成。
件数一致・全件CSV出力（`allEventsCsvText()`）の動作・Playwrightでのモバイル幅実機確認まで行う。
※ データは同一テーブル（ratings）に `municipality_id` で分離して蓄積される。

**Step 4：カードの説明（ガイド画面）の実装（2026-07-09追加・6自治体で確立済みの標準機能）**

評価カード画面のヘッダーに「📖 カードの説明」リンクを常設し、カードの各項目が評価シートのどこから来て
どう加工されているか（転記／区分・分類／AI分類／AI要約／固定表示の5分類）を、評価シートを起点にした
対応表で説明する専用画面（`renderGuide()`）。詳細な設計判断・自治体ごとの実例は
`project_gyosei_hyoka_card_description.md`メモリを参照。

*実装（コピーで済む部分）*：`build_kitamoto_v3.py`または`build_nishinomiya_v3.py`から以下5点をそのまま移植する。
1. CSS：`.tproc-*`（処理タイプバッジ5種）・`.gintro`・`.gproc-legend`・`.gtbl`・`.grow`・`.gc-item`/`.gc-proc`/`.gc-src`・`.gmap`・`.gitem`/`.glbl`
   （**3列の実テーブル＋レスポンシブ切り替えは不採用**。モバイル・PC双方で窮屈になることが西宮市の実験で判明したため、
   画面幅によらず「項目名＋バッジが1行、説明がその下」という縦積みブロックに統一している。このCSSをそのまま使うこと）
2. `procBadge(label)` / `convRow(k, v)` ヘルパー関数
3. `renderGuide(app)` 関数の骨格（見出し・凡例・表構造・末尾の評価ボタン説明・末尾の`.sticky-actions`固定フッター）
4. `render(screen)` ディスパッチャへの `else if (screen === 'guide') renderGuide(app);` 追加
5. 評価カード画面（`renderEval`）ヘッダーへの「📖 カードの説明」リンク追加（`.hd`の濃紺背景に合わせ、白系のピル型スタイルにすること。本文と同じ色にすると見えなくなる事故が実際に発生した）

*投資が必要な部分（自治体ごとに毎回やり直す）*：`renderGuide()`内の各項目の説明文・組替表の中身。
1. 対象自治体の`extract_{name}_v2.py`（または`extract_{name}.py`）を読み、no/name/dept/root/ev/budget/cls/
   overview/output_name/outcome_name等が、それぞれ元データのどのフィールド・どのラベルから来ているか、
   どんな変換ロジック（マッピング辞書・条件分岐）を経ているかを特定する
2. **必ず実物のPDF/Excelを画像として確認してから書く**（[[reference-pdf-visual-render]]参照：
   `fitz.open(path).load_page(n).get_pixmap(dpi=150).save(png)` → Readツールで確認）。
   スクリプトのコメント・docstringだけを信じて書かない。西宮市では「市の関与の妥当性」の実際の選択肢文言、
   富田林市・聖籠町では回答スタンプ/座標抽出の元になった設問文が、実画像を見て初めて正確に判明した
3. root・evのように、評価シートの決まった選択肢からタグへ変換している項目（処理タイプ＝区分・分類）は、
   `convRow()`を使った組替表（選択肢の原文→変換後タグ）を必ず作る。変換ロジックが自明でない場合
   （例：品川区はrootとevが同じ欄から来る、聖籠町は「法定義務あり」という判定が存在しない等）は、
   その旨を文章でも明記する
4. cls（施策）が評価シートに存在しない自治体（聖籠町方式）の場合、「評価シートには施策欄が存在しません」
   と明記した上で、代替データソース（総合計画等）へのリンクをgc-src内に追加することを検討する
   （聖籠町では総合計画PDFのURLを`<a>`タグで埋め込んだ）
5. 実装後、Playwrightでモバイル幅（390px）のスクリーンショットを確認し、コンソールエラーがないこと・
   画面遷移後に評価キューの位置が保持されること（`render('eval')`で戻れること）を確認する
6. **表の内容は、実装前に必ずユーザーへ table 形式で提示し、追加調査の要否を確認してから書き始めること。**
   決算額のフォールバックロジック等、コードを読んだだけでは「実際に発動しているか」が分からないケースがある
   （北本市で発覚した潜在バグの実例）。疑わしい場合はPDF画像や実データでの検証を提案する

**品質チェック（フェーズ④へ進む前に必須）**

1. **機械的チェック（03_パッチ済みデータ\story_gen_prompt.md参照）**：outcome_nameが実在するのに「設定されていません」と誤記載していないか（センチネル値「データなし」「成果指標なし」等は判定から除外すること）／逆方向の誤記載（センチネル値なのに「設定されています」と書いていないか）／年度表現（令和◯年度等、story_p1・story_p2の両方）／コスト金額表現（千円等）を正規表現で全件スキャン。**抽出ロジックの修正等で後からCSVの値を書き換えた場合は、影響行のstory_p1/p2を空欄化し、03_パッチ済みデータ\story_gen_prompt.md最新版のプロンプト（is-not-set禁止・年度表現禁止・コスト表現禁止を含む）で再生成すること。** 古いプロンプトのコピー（`gen_story_XX.py`が更新前のテンプレートをコピーしたものである等）を使うと、他自治体で既に発見・修正済みのバグが再発する（湖西市で実例あり）
2. **「なじまない」等の除外ワード分類は目視で全件確認する**（富田林市の教訓）：outcome_nameに「〜になじまない」を含む行を機械的に「正常な指標なしケース」として除外すると、「目標値の設定はなじまない」（指標自体は実在）のような、意味の異なるケースを誤って見逃すことがある。除外対象になった行を一度全件ダンプして目視確認すること
3. **実機比較レビュー（必須）**：機械的チェックが通っても、生成されたカードと元のPDF/Excelのスクリーンショットを実際に並べて数件〜十数件比較すること。富田林市では以下2件の不具合が診断スクリプトでは検出できず、この実機比較で初めて見つかった：
   - 活動指標名が長く2行に折り返るPDFで、ラベルがテキストの中間に挟まり名前が空文字列になっていた（`extract_indicators()`のバグ）
   - AI生成テキストで1文字の漢字が無関係な外国語に置き換わる文字化け（「消火設備」→「消fire設備」等）。低頻度（0.36%程度）だが機械チェックでは拾いにくいため、`unicodedata`で日本語スクリプト範囲外の文字を全フィールドで走査するチェックも有効
   - 目安：ランダムまたは先頭から10〜20件をPlaywright等でカード表示し、元データと突き合わせる

---

### フェーズ④ 公開

**このフェーズは冒頭「環境設定」で選んだプリセットにより内容が分かれる**：
- ①ハブ運用：以下の「ハブ登録フォーム」に従い、GitHub公開先（環境設定）＝ハブリポジトリのルートに配置し、ハブの`MUNICIPALITIES`配列にも登録する
- ②独立運用：ハブ登録は行わない。GitHub公開先（環境設定）＝自分の新規リポジトリのルートにHTMLを配置するのみ。具体的な手順（新規リポジトリ作成・Supabase新規プロジェクト作成・プレースホルダー置換等）は`スターターキット.md`の「0〜6手順」ステップ6を参照

**ハブ登録フォーム（①のみ・空欄を埋める）**

```javascript
{
    pref:         '',  // 都道府県名（例：'新潟県'）
    name:         '',  // 市区町村名（例：'聖籠町'）
    year:         '',  // 評価年度（例：'令和6年度'）
    count:        0,   // 事業件数
    file:         '',  // ファイル名（例：'seiro_index.html'）
    source_url:   '',  // データ出典URL
    source_label: '',  // 出典ラベル（例：'令和6年度事業の政策評価シート'）
}
```

`04_本番HTML生成\{name}_v3_index.html`（ビルド直後の出力）を直下の`{name}_index.html`（本番ファイル）へ
コピーしてから、GitHub公開先（環境設定＝`jimujigyou-hyoka` リポジトリ）のルートに配置する。
※ このコピーは自動化されていない。コピー漏れの事故が過去に複数回発生しているため、
アップロード前に必ず直下のファイルと`04_本番HTML生成`の出力のハッシュが一致するか確認すること。
ハブ（`index.html`）の `MUNICIPALITIES` 配列に上記オブジェクトを追加。

---

## 注意事項

- PowerShellはインライン実行のみ（`.ps1` ファイルは日本語パスが文字化けするため禁止）
- Windowsで日本語を含む作業フォルダを扱う場合、Pythonの実行はGit Bashではなく PowerShell 経由で行う（Git Bashだとcp932/UTF-8のエンコーディング不一致で`cd`・実行結果表示が文字化け・失敗することがある。2026-08-04・朝来市実地テストで発覚）
- 出力ファイル命名：`{自治体名ローマ字}_index.html`（略称不可）
- LocalStorageキー：`{ローマ字}{年度下1桁}_ratings`（例：`seiro6_ratings`）
- CSV出力はBOM付きUTF-8（`'﻿'` をヘッダー先頭に付与）
- story_p1・story_p2 の生成は `03_パッチ済みデータ\story_gen_prompt.md` を参照
- **人間への確認は、自己評価の報告だけで済ませない**（2026-08-04追加・朝来市実地テストで発覚）：「少数件を試作して品質確認しました、残り全件に進めてよいですか」のように可否だけを聞くのではなく、判断材料となる実際の生成物（サンプル全文、または該当ファイルの場所）を人間に提示した上で了承を取る。人間が中身を見ずに選択肢を選ぶだけの形にしない
- **マッピング表は項目を1つずつ名指しして確認を取る（一覧表を見せるだけで済ませない）**（2026-08-04追加・朝来市実地テストで発覚）：カード項目（事業名／事業番号・担当課／施策／法定根拠タグ／行政評価タグ／提供形態タグ／目的・事業内容／決算額／人件費／成果／廃止した場合の影響）が元シートのどこから来て、どう加工されるか（転記／区分・分類／AI分類／AI要約／固定表示）を、フェーズ①（抽出方式判定）の直後・フェーズ②の重い処理（story_p1/p2等の全件生成）に着手する前に、**項目ごとに「評価カード『事業名』は評価シート『事務事業名』でよろしいですか？」のように名指しで確認する**。直接対応が明らかな項目（事業名等）も省略しない。一覧表としてまとめて提示するだけだと、AIが「確認不要」と暗黙に判断した項目が漏れたまま流れてしまう（朝来市で実例あり：法定根拠タグが元シートに対応欄がないため447件全件「データなし」になっていたが、確認のタイミングが一度もなく、人間が後から気づいて発覚した）。この一覧はビルド後の「カードの説明」画面と同内容なので、先に作った草案をそのまま再利用してよい。特にstory_impact（廃止した場合の影響）のように、元シートに対応する専用欄がなく代替の欄で代用する必要がある項目は、この段階で必ず実例（実際のシートの該当欄のテキスト）付きで確認を取る。story_impactは特に、ev区分に偏らず（拡大〜廃止方向まで）複数件のサンプルを見せること（`ev=廃止方向`の行だけ検算すると、それ以外の区分に紛れた誤判定を見逃す。朝来市で実例あり：詳細は上記「story_impact 抽出方針」参照）。root（法定根拠タグ）のように**全件が同じ値になる**場合は、それ自体が「専用欄がそもそも存在しない」という構造的な状況を示す強いシグナルなので、なおさら確認が必要

---

## 継続的改善（セルフレビュー）

自治体版のビルドが一区切りついたら（少なくともフェーズ③終了時点、理想はフェーズ④公開後）、このセッション自身の会話ログを振り返り、今回の作業で見つかった問題点・改善余地を洗い出す。

- 自分のセッションログ（`~/.claude/projects/<プロジェクトパス>/<セッションID>.jsonl`）を読み、ツールエラー・手戻り・人間確認なしに進めてしまった判断・非効率な試行錯誤等を探す
- 見つかった項目は、そのまま本ファイルを書き換えるのではなく、**必ず人間に一覧で提示し、確認を得てから**このファイルの末尾に追記する（自己判断で書き換えない。理由は上記「注意事項」の各原則と同じ）
- 追記は本文の構造を崩さないよう、新しい節として日付付きで積み重ねる（例：`## 2026-08-10・◯◯市ビルドで得た知見`）
- 各自のローカルコピーはこの積み重ねにより自治体ごとに独自の知見を蓄積していく。全体で共有したい重要な発見があれば、AIが`gh` CLIでIssue投稿を代行できる（🤝 AI→人間：Issueの投稿内容を一緒にまとめ、確認を得てから`gh issue create`で元のスターターキットリポジトリ（`https://github.com/onuma-spec/jimujigyou-hyoka-starter-kit`）に投稿する。`gh`が未インストールの場合はインストールを提案してよいが、投稿は必ず内容確認後に実行する）
